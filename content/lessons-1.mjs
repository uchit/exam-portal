// Generated by `node scripts/export-content.mjs` from Redis (content:lessons:1).
// Edit content in Redis and re-run the export, not this file directly.
export const LESSONS = [
  {
    "slug": "1-1-agentic-loop",
    "title": "The Agentic Loop and stop_reason",
    "dek": "How the model-tool-client loop actually terminates, and why stop_reason is the only signal you can trust to control it.",
    "minutes": 7,
    "bodyHtml": "\n      <p>Every agentic system built on the Claude API runs the same underlying loop: the model receives the conversation so far, decides whether to call a tool, the client executes that tool and appends the result, and the whole thing repeats. There is no magic scheduler underneath this. The loop is just a while-loop in your own code, and the entire question of \"when do we stop\" comes down to one field on the API response: <code>stop_reason</code>.</p>\n      <p>On each turn, Claude returns a response with a <code>stop_reason</code> that tells you why generation stopped. The four values you need to know cold for the exam are:</p>\n      <ul>\n        <li><code>end_turn</code> — the model believes it has finished responding to the user. No further tool calls are pending. This is your signal to exit the loop and hand control back to the human.</li>\n        <li><code>tool_use</code> — the model wants to call one or more tools. You must execute them and feed the results back as a new user turn containing <code>tool_result</code> blocks before continuing.</li>\n        <li><code>max_tokens</code> — generation was cut off because it hit the token limit, mid-thought. This is not a natural stopping point; the response may be truncated JSON or a half-written tool call.</li>\n        <li><code>stop_sequence</code> — the model hit a custom stop sequence you configured. Also not a natural end of task.</li>\n      </ul>\n      <p>The correct control-flow pattern is simple: after every API call, branch on <code>stop_reason</code>. If it's <code>tool_use</code>, execute the requested tools and loop again. If it's <code>end_turn</code>, exit. If it's <code>max_tokens</code> or <code>stop_sequence</code>, treat it as an error condition — log it, possibly retry with adjusted parameters, but do not silently continue as if the task finished normally.</p>\n      <h2>Why not just read the text?</h2>\n      <p>A tempting shortcut is to look at the assistant's text output and guess whether it \"sounds done\" — scanning for phrases like \"Task complete\" or \"I have finished.\" This fails for several concrete reasons. Models phrase completion inconsistently across turns. A response can contain both prose and a tool call in the same turn, so text presence tells you nothing about whether more tool calls are coming. And relying on natural-language parsing couples your control flow to model phrasing, which changes across model versions and is not a contract Anthropic guarantees. <code>stop_reason</code> is a structured, documented field specifically designed to be machine-readable; text content is not.</p>\n      <div class=\"callout\"><b>Exam trap</b> Expect a question describing a loop that checks <em>whether the response contains a tool_use content block</em> versus <em>whether stop_reason equals tool_use</em>, and asks which is correct. Both can look equivalent at first glance, but the robust pattern is branching on <code>stop_reason</code> directly — checking for a content block type is a weaker, indirect proxy and the exam will present a scenario where it diverges (e.g., a malformed or truncated response) from the stop_reason-based check.</div>\n      <h2>Iteration caps are a safety net, not a control mechanism</h2>\n      <p>Production agent loops should include a maximum iteration count. But its job is to prevent runaway cost and infinite loops when something goes wrong upstream — not to be the primary way you decide the task is done. If your loop's actual termination logic is \"stop after N iterations regardless of stop_reason,\" you have built a timer, not an agent. The correct design uses <code>stop_reason == \"end_turn\"</code> as the primary exit condition and an iteration cap purely as a defensive ceiling that should, in a correctly functioning system, almost never be the thing that actually ends the loop.</p>\n      <blockquote>Scenario: An agent is wired to call a search tool repeatedly. On iteration 40 of a 50-iteration cap, the model returns a response with no tool_use blocks and stop_reason of end_turn, but the assistant text happens to include the word \"next\" because it's summarizing next steps for the user. A text-scanning implementation misreads this as \"more work to do\" and keeps looping past a completed answer. The correct implementation exits immediately because stop_reason is end_turn, regardless of the words used in the response.</blockquote>\n      <h2>Try it</h2>\n      <p>Using the Claude API directly (not the SDK's agent loop helpers), write a bare tool-use loop for a single tool of your choice. Log the <code>stop_reason</code> on every turn. Then deliberately set <code>max_tokens</code> low enough that a tool call gets truncated, and observe what <code>stop_reason</code> comes back and how your loop should react to it versus how it reacts if you naively assume any non-empty response means success.</p>\n    "
  },
  {
    "slug": "1-2-orchestration-patterns",
    "title": "Orchestration Patterns: Chaining, Routing, Parallelization",
    "dek": "The core workflow patterns from Anthropic's agent-building guidance and when each one is the right tool for a task.",
    "minutes": 8,
    "bodyHtml": "\n      <p>Not every agentic task needs a full autonomous agent loop. Anthropic's guidance on building effective agents draws a useful line between workflows, where the control flow is defined by your code, and agents, where the model decides its own path through a task. Three workflow patterns cover most of the ground: prompt chaining, routing, and parallelization. Knowing which one fits a given scenario — and being able to say why the alternatives are worse — is a recurring exam theme.</p>\n      <h2>Prompt chaining</h2>\n      <p>Prompt chaining is a fixed sequence of LLM calls where each step's output becomes the next step's input. It's the workflow equivalent of a Unix pipe. You use it when a task decomposes into an ordered set of subtasks that are known in advance and where each stage benefits from focused, narrower attention rather than asking one call to do everything at once. A classic example is generate an outline, then expand the outline into a draft, then edit the draft for tone — three calls, each one refining the last, in a sequence that doesn't change based on the input.</p>\n      <p>The key property of prompt chaining is predictability: you know at design time exactly how many steps there are and what order they run in. If you find yourself needing to skip steps conditionally based on intermediate results, you've drifted from pure chaining toward routing or a more agentic design.</p>\n      <h2>Routing</h2>\n      <p>Routing classifies the input first, then sends it down one of several specialized downstream paths. Each path can have its own prompt, its own tools, even its own model. Use routing when your inputs fall into genuinely distinct categories that benefit from separate handling — a customer support system routing billing questions to one prompt and technical issues to another is the textbook case. The classification step itself is usually a small, cheap call (or even a simple classifier) whose only job is picking a lane.</p>\n      <p>The failure mode to watch for is applying routing when categories aren't actually distinct enough to need separate handling — in that case you've added a classification step and a maintenance burden for no real gain over a single well-written prompt.</p>\n      <h2>Parallelization</h2>\n      <p>Parallelization runs multiple LLM calls at once and comes in two flavors. Sectioning splits a task into independent subtasks that are dispatched simultaneously and then combined — for example, reviewing different sections of a large codebase for different concern types (security, performance, style) at the same time. Voting runs the same task multiple times, often with varied prompts or sampling, and aggregates the results for consensus — useful when you want higher confidence on a judgment call than a single pass reliably gives you, such as flagging whether content violates a policy.</p>\n      <p>Sectioning buys you speed and focus; voting buys you reliability at the cost of extra calls. The exam will expect you to distinguish \"split the work\" from \"repeat the work for confidence\" as two different reasons to reach for parallelization.</p>\n      <div class=\"callout\"><b>Exam trap</b> A scenario describes independent, predictable subtasks (e.g., translate this document into five languages) and the wrong-answer options include an orchestrator-workers setup. Orchestrator-workers is for when the decomposition isn't known ahead of time and needs to be figured out dynamically by a lead agent. When the subtasks are fixed and independent in advance, plain parallelization (sectioning) is the simpler, correct answer — don't reach for a dynamic orchestrator when a static fan-out will do.</div>\n      <blockquote>Scenario: A team wants to build a pipeline that takes a support ticket, determines whether it's a billing, technical, or account-access issue, and then handles each category with a very different tone and tool set. This is routing: the categories are genuinely distinct, known in advance, and each needs specialized handling. It is not prompt chaining, because there's no fixed sequence of transformations applied uniformly to every ticket — the path itself changes based on the input.</blockquote>\n      <h2>Try it</h2>\n      <p>Pick a task you'd normally do in a single long prompt — say, \"summarize this article and translate the summary into three languages.\" Rebuild it as an explicit prompt chain (summarize, then three parallel translation calls) using the Claude API directly, with each step's output logged. Compare the reliability and clarity of the output against the single-call version.</p>\n    "
  },
  {
    "slug": "1-3-subagent-design",
    "title": "Subagent Design and Context Passing",
    "dek": "What a subagent actually returns to its caller, how much context it should get, and why isolation is the default, not the exception.",
    "minutes": 8,
    "bodyHtml": "\n      <p>A subagent is not a shared thread with the orchestrator — it's a separate agentic loop with its own context window, invoked for a bounded task, that reports a result back. Two design decisions dominate whether a subagent architecture works well: how much context the subagent receives on the way in, and how much it returns on the way out.</p>\n      <h2>What goes back to the parent</h2>\n      <p>When a parent agent invokes a subagent, it does not receive the subagent's full transcript — the intermediate tool calls, the reasoning, the false starts. It receives a final result: a distilled answer to the question or task it was given. This matters architecturally because it's what makes subagents useful for context management. If the parent absorbed every subagent's full internal transcript, you'd gain nothing over just doing the work in the parent's own context — you'd just be paying more calls to reach the same context bloat.</p>\n      <p>This also means subagent design is partly a prompt-engineering problem for the return value. A subagent that returns \"done, see above\" is useless to a parent that never sees \"above.\" A well-designed subagent is instructed to produce a self-contained final summary: what it did, what it found, and any caveats — because that summary, and nothing else, is what the orchestrator has to work with afterward.</p>\n      <h2>Context isolation on the way in</h2>\n      <p>The complementary decision is what the subagent receives when it's invoked. The default should be context isolation: give the subagent only what it needs for its specific subtask, not the orchestrator's entire history. Dumping the full parent context into every subagent call defeats much of the purpose of decomposing the task in the first place — you're back to one giant context, just split across multiple calls that each pay for the same tokens.</p>\n      <p>Isolation also has a quality benefit beyond token cost: a subagent handed a narrow, well-scoped brief tends to stay focused on its task, while a subagent handed the entire conversation history has to figure out which parts are relevant to it, which invites drift and irrelevant tangents in its output.</p>\n      <p>The skill here is judging what \"only what it needs\" actually means for a given subtask. Too little context and the subagent can't do its job or has to re-derive things the parent already knows. Too much and you've reintroduced the bloat isolation was supposed to prevent. The right amount is usually: the specific subtask description, any directly relevant facts or file contents, and explicit output-format expectations — not the full chat log that led to this point.</p>\n      <div class=\"callout\"><b>Exam trap</b> A question describes a subagent invocation where the orchestrator forwards its entire conversation history \"to be safe\" and asks whether this is good practice. It is not, even though it feels safer. The correct answer favors deliberately scoped context passing — the exam is testing whether you understand that more context is not free and that isolation is a design principle, not a corner case to handle only when tokens run low.</div>\n      <blockquote>Scenario: An orchestrator is researching a technical question and dispatches a subagent to read and summarize a 40-page specification document. The subagent should be given the document (or a path to it) and a specific question to answer, not the orchestrator's prior five conversation turns about an unrelated part of the project. When the subagent finishes, it should return a short, self-contained answer to the specific question — not a transcript of every section it read along the way.</blockquote>\n      <h2>Naming and describing subagents</h2>\n      <p>In systems where an orchestrator chooses which subagent to invoke, the subagent's name and description function as its interface. The orchestrator picks a subagent the same way it picks a tool: based on a short description of what it does and when to use it. A vague description (\"helper agent\") gives the orchestrator nothing to route on and leads to the wrong subagent being picked, or the orchestrator falling back to doing the work itself. A specific description (\"reviews code diffs for security vulnerabilities; does not check style or performance\") lets the orchestrator dispatch correctly without inspecting the subagent's internals.</p>\n      <p>This matters more as the number of available subagents grows. With two or three subagents, ambiguity is survivable. With a dozen, overlapping or vague descriptions cause the orchestrator to misroute tasks in ways that are hard to debug after the fact, because the failure looks like a bad result rather than an obviously wrong routing decision.</p>\n      <h2>Single-responsibility subagents</h2>\n      <p>A subagent that tries to do several unrelated things is harder to prompt well, harder to test, and harder for an orchestrator to route to correctly. The same single-responsibility instinct that applies to functions in regular software applies here: a subagent scoped to \"run tests and interpret failures\" is easier to reason about and reuse than one scoped to \"handle all quality assurance,\" which quietly expands to include linting, security scanning, and documentation review over time. Keep subagent scope narrow enough that its name alone tells you what it does and, just as importantly, what it doesn't do.</p>\n      <div class=\"callout\"><b>Exam trap</b> A question describes a single subagent handling several unrelated responsibilities (say, code review, deployment, and documentation generation) and frames this as efficient because it avoids the overhead of multiple invocations. The exam expects you to flag this as a design smell — a narrowly scoped subagent per responsibility is easier to test, describe accurately for routing, and reason about when something goes wrong, and the overhead of an extra invocation is rarely the dominant cost compared to the debugging cost of an overloaded subagent.</div>\n      <h2>Try it</h2>\n      <p>In Claude Code or via the API, set up a small orchestrator-subagent pair: have a parent prompt dispatch a subagent with only a narrow, specific instruction and a single relevant file, then print exactly what comes back to the parent. Compare that against giving the same subagent your full running conversation as context, and notice how the final summary changes in focus and length.</p>\n    "
  },
  {
    "slug": "1-4-parallel-vs-sequential",
    "title": "Parallel vs. Sequential Execution",
    "dek": "How to decide whether subtasks should run one after another or all at once, and what breaks when you get it wrong.",
    "minutes": 7,
    "bodyHtml": "\n      <p>Once a task is decomposed into subtasks, you still have to decide how to schedule them. The decision is almost always a dependency question, not a preference question: if subtask B needs the output of subtask A, they must run sequentially. If two subtasks are independent of each other, running them in parallel is usually strictly better — same result, less wall-clock time.</p>\n      <h2>The dependency test</h2>\n      <p>Before parallelizing anything, ask: does this step's input depend on another step's output? If yes, that's a sequential edge and no amount of infrastructure lets you skip it — the data literally doesn't exist yet. Prompt chaining is sequential by definition because each step depends on the last. Sectioning-style parallelization is safe specifically because the sections were chosen to be independent in the first place; the independence is what licenses the parallel execution, not the other way around.</p>\n      <p>A subtler version of this shows up when subtasks look independent but actually share a hidden dependency — for example, two subagents each trying to write to the same file, or two subagents each expected to make the \"final\" decision on something only one of them should decide. These aren't data dependencies in the obvious sense, but they're still dependencies, and running them in parallel produces race conditions or contradictory outputs rather than a speedup.</p>\n      <h2>Cost and latency tradeoffs</h2>\n      <p>Parallel execution reduces wall-clock latency but does not reduce total token spend — if anything it can increase it, since parallel subagents each carry their own context overhead rather than sharing one growing context. Voting-style parallelization is the clearest example: running the same task five times for consensus costs roughly five times the tokens of running it once, in exchange for higher confidence in the result. That tradeoff is worth it when the cost of a wrong answer is high and single-pass reliability is shaky; it's wasteful when a single well-prompted call is already reliable enough.</p>\n      <p>Sequential chains, by contrast, tend to be more token-efficient per subtask but pay for that in latency — the total time is the sum of every step, not the max of the slowest one.</p>\n      <div class=\"callout\"><b>Exam trap</b> A scenario presents subtasks that appear independent on the surface (e.g., \"have three subagents each update a shared summary document\") and asks whether they should run in parallel. The trap is treating apparent task-independence as sufficient justification for parallelism when there's a shared, mutable resource underneath. The correct answer recognizes the write conflict and either sequences the updates or has a single agent own the merge step — independence of the task description is not the same as independence of the actual side effects.</div>\n      <blockquote>Scenario: A code review agent needs to check a pull request for security issues, style violations, and performance regressions. These three checks read the same diff but write to entirely separate output sections and don't depend on each other's findings. This is a clean case for sectioning-style parallelization — dispatch three subagents at once, each scoped to one concern, and merge their independent outputs. Contrast this with a task where step two needs to know whether step one found any security issues before deciding whether to run at all — that dependency forces sequencing.</blockquote>\n      <h2>Rate limits and resource contention</h2>\n      <p>Parallel execution is not free of constraints even when the tasks are logically independent. Firing off a large number of concurrent API calls can hit rate limits, and a system that fans out to, say, fifty parallel subagents for a task that only needed five may spend more time backing off from throttling than it saves in wall-clock latency. Practical parallel designs cap concurrency deliberately — batching parallel calls in groups, or bounding the number of simultaneous workers an orchestrator will dispatch at once — rather than assuming \"independent\" means \"safe to run all at once with no limit.\"</p>\n      <p>The same caution applies to shared external resources beyond the model API itself: a database, a file system, or a third-party service with its own rate limits. Two subagents can be logically independent in terms of the data they read and write, yet still compete for the same underlying resource capacity, degrading each other's latency even without a correctness bug.</p>\n      <h2>When sequential is simply safer</h2>\n      <p>Beyond hard dependencies, there are cases where sequential execution is the pragmatic choice even without a strict data dependency: when subtasks are cheap enough that parallel infrastructure adds more complexity than it saves, when debugging a single linear trace is significantly easier than reconciling five concurrent traces, or when the task is exploratory and you expect to change your approach after seeing the first result. The exam frames this as a judgment call weighing latency savings against complexity and debuggability, not a rule that independent tasks must always be parallelized.</p>\n      <div class=\"callout\"><b>Exam trap</b> A scenario shows subtasks that are independent and asks whether to parallelize, with \"always parallelize independent tasks to save time\" offered as a tempting general rule. Independence is necessary but not sufficient — rate limits, shared downstream resources, and the added complexity of reconciling concurrent results are all legitimate reasons to keep independent work sequential. The exam expects you to weigh the tradeoff rather than apply parallelization as a blanket default.</div>\n      <h2>Try it</h2>\n      <p>Take a multi-part task with at least one real dependency (for example: fetch data, then analyze it, then two independent summaries of the analysis for different audiences). Implement it with the dependent steps sequential and the two summaries dispatched in parallel via separate API calls fired concurrently. Time the parallel portion against running those same two summary calls sequentially to see the latency difference directly.</p>\n    "
  },
  {
    "slug": "1-5-multi-agent-systems",
    "title": "Multi-Agent System Design",
    "dek": "The orchestrator-workers pattern, hub-and-spoke topology, and why subagents don't talk to each other directly.",
    "minutes": 8,
    "bodyHtml": "\n      <p>When a task's decomposition can't be fully planned in advance — the set of subtasks depends on what earlier subtasks discover — a static workflow like prompt chaining or fixed parallelization isn't flexible enough. This is the situation the orchestrator-workers pattern is built for: a lead agent examines the task, decides how to break it up (potentially adjusting as it goes), dispatches worker subagents for the pieces, and synthesizes their results into a final answer.</p>\n      <h2>Orchestrator-workers versus static parallelization</h2>\n      <p>The distinguishing feature of orchestrator-workers is that the decomposition itself is a judgment call made at run time by the lead agent, not a fixed plan written into your code ahead of time. A research task where the number and topic of sub-questions depends on what the first pass of research turns up is a natural fit — you can't know in advance how many workers you'll need or what to tell each one until the orchestrator has looked at the problem. Compare this to sectioning-style parallelization, where the split is already known before any agent runs (e.g., \"check these five files\"); there, you don't need an orchestrating agent making decisions, you just need a fixed fan-out.</p>\n      <p>Use orchestrator-workers specifically when subtask decomposition isn't fully predictable in advance. If you can enumerate the subtasks at design time, you don't need the overhead and unpredictability of a dynamic orchestrator — use a simpler static pattern instead.</p>\n      <h2>Hub-and-spoke topology</h2>\n      <p>In an orchestrator-workers system, subagents report only to the orchestrator — never to each other. This is the hub-and-spoke topology, and it's a load-bearing design constraint, not an incidental detail. Worker subagents don't know about each other's existence, don't coordinate directly, and don't pass messages peer to peer. All communication flows through the hub: the orchestrator dispatches work out to the spokes and receives results back in.</p>\n      <p>This constraint exists because peer-to-peer agent communication multiplies the ways a multi-agent system can fail — race conditions, circular dependencies, inconsistent shared state, and debugging complexity all grow fast once agents can talk to each other directly rather than through a single coordinating point. Hub-and-spoke keeps the failure surface centralized: if something goes wrong, it's traceable to either a worker's output or the orchestrator's synthesis, not to an emergent interaction between two workers that never should have known about each other.</p>\n      <div class=\"callout\"><b>Exam trap</b> A design question proposes letting two subagents exchange information directly to \"save a round trip\" through the orchestrator, framed as an efficiency improvement. This breaks hub-and-spoke topology and is the wrong answer even when it sounds more efficient — the correct design routes that information through the orchestrator, which can also apply judgment about whether and how to pass it along, rather than letting workers coordinate as unsupervised peers.</div>\n      <blockquote>Scenario: A code migration task requires first surveying a codebase to find all the places a deprecated API is used, and the number and nature of the required fixes can't be known until that survey completes. The orchestrator dispatches a survey subagent, receives back a list of affected files, and then dynamically dispatches one worker subagent per affected file to apply the fix — a count and set of tasks that wasn't knowable before the survey ran. Each worker reports its result back to the orchestrator, which compiles a final change summary; no worker ever contacts another worker.</blockquote>\n      <h2>The orchestrator's synthesis responsibility</h2>\n      <p>Dispatching workers is only half of the orchestrator's job; the other half is synthesis. Worker results often disagree, overlap, or arrive in a form that needs reconciling before they're useful to the end user — two workers might flag the same underlying issue from different angles, or one worker's finding might contradict another's assumption. A weak orchestrator implementation just concatenates worker outputs and calls it a final answer. A correct implementation treats synthesis as real work: resolving conflicts, deduplicating overlapping findings, and producing a single coherent result rather than a stitched-together list of everything the workers happened to say.</p>\n      <h2>Evaluator-optimizer as a related but distinct pattern</h2>\n      <p>It's worth distinguishing orchestrator-workers from the evaluator-optimizer pattern, since exam scenarios sometimes blur the two. In evaluator-optimizer, one call generates a candidate solution and a second call evaluates it against explicit criteria, feeding back specific critique; this loops until the output passes. It's suited to tasks where quality criteria are clear and checkable but a single generation pass isn't reliably good enough — think of a draft-and-critique loop for a piece of writing that has to satisfy a strict rubric. Orchestrator-workers, by contrast, is about decomposing a task into independent pieces of work run by different subagents, not about iteratively refining one piece of work through generate-then-critique cycles. Both involve more than one model call working on the same overall goal, but the shape of the collaboration — decomposition versus refinement — is different, and the exam expects you to tell them apart from a scenario description.</p>\n      <div class=\"callout\"><b>Exam trap</b> A scenario describing a generate-then-critique-then-revise loop (one call writes, another checks against criteria and sends it back for revision) is sometimes mislabeled as orchestrator-workers because \"there's more than one agent involved.\" This is evaluator-optimizer, not orchestrator-workers — orchestrator-workers decomposes a task into independent worker subtasks, while evaluator-optimizer iteratively refines a single piece of output through feedback. Confusing the two is a common wrong-answer pattern.</div>\n      <h2>Try it</h2>\n      <p>Build a two-level orchestrator-workers setup in Claude Code: have a lead prompt inspect a small codebase, decide how many and which files need a specific kind of review, and dispatch one subagent per file it chooses (not a fixed number you hardcode). Confirm each worker's result flows back only to the orchestrator, and have the orchestrator write the final combined report itself.</p>\n    "
  },
  {
    "slug": "1-6-failure-modes",
    "title": "Failure Modes in Agentic Loops",
    "dek": "The concrete ways agentic loops break in production, and the anti-patterns the exam expects you to recognize and reject.",
    "minutes": 8,
    "bodyHtml": "\n      <p>Agentic loops fail in a small number of recurring, well-understood ways. The exam tests whether you can spot these failure modes in a scenario description and identify the correct fix, so it's worth cataloguing them explicitly rather than treating \"the agent got stuck\" as one undifferentiated problem.</p>\n      <h2>Loop control anti-patterns</h2>\n      <p>The most fundamental failure mode is using the wrong signal to control the loop. As covered earlier, <code>stop_reason</code> is the only reliable way to know whether the model is done or wants another tool call. Three specific anti-patterns recur on the exam: parsing the assistant's natural-language text to infer intent instead of checking <code>stop_reason</code>; relying on an iteration cap as the primary way the loop ends, rather than as a safety net; and checking for the presence of text content as a proxy for completion instead of checking the actual <code>stop_reason</code> value. All three substitute a fuzzy, indirect heuristic for a direct, documented signal, and all three fail in scenarios that are easy to construct — truncated responses, mixed text-and-tool-call turns, or unusually-phrased completions.</p>\n      <h2>Runaway loops and cost blowouts</h2>\n      <p>Even with correct <code>stop_reason</code> handling, a loop can fail to converge — the model keeps calling tools without making progress, perhaps because a tool is returning an error it keeps retrying the same way, or because the task was underspecified and the model can't tell it's actually finished. This is exactly what the iteration cap safety net is for: a hard ceiling that terminates the loop and surfaces an error rather than letting it run (and bill) indefinitely. The distinction to hold onto is that the cap being hit is itself a signal something went wrong — it should never be silently treated as \"task complete.\"</p>\n      <h2>Context and state failures</h2>\n      <p>Long-running agentic loops accumulate context. Every tool call and result gets appended to the conversation, and eventually this causes real problems: relevant early instructions get buried or fall out of the effective attention window, token costs climb, and latency per turn increases as the context grows. In multi-agent systems, a related failure is context leakage in the other direction — a subagent given too much of the orchestrator's history can get confused about which parts are relevant to its specific subtask, degrading its output even though nothing about the loop mechanics is technically broken.</p>\n      <h2>Tool-result and error handling failures</h2>\n      <p>A tool call can fail — a bad argument, a downstream service error, a malformed input the model generated. If the loop doesn't feed that failure back to the model as a clear <code>tool_result</code> (including error state), the model has no way to know its last action didn't work and will often either repeat the same broken call or hallucinate a result and proceed as if it succeeded. Correct error handling surfaces the failure explicitly in the tool result so the model can adapt — retry differently, ask for clarification, or report the failure upward — rather than silently swallowing it or crashing the whole loop on the first error.</p>\n      <div class=\"callout\"><b>Exam trap</b> A scenario describes a loop that has been running for many iterations without producing a final answer, and offers \"increase the iteration cap\" as a fix. This is almost always the wrong answer on its own — a runaway loop is a symptom of a control-flow or tool-feedback bug (bad stop_reason handling, a tool silently failing and being retried, an underspecified task), and raising the cap just delays the failure and increases cost without addressing the cause. The correct answer diagnoses why the loop isn't converging, not how much longer to let it run.</div>\n      <blockquote>Scenario: An agent calling a file-write tool keeps calling the same tool with the same arguments for ten consecutive turns without the task advancing. Investigation shows the tool is returning a permissions error each time, but the client code was only forwarding successful tool outputs into the conversation and dropping errors, so the model never saw that its calls were failing and had no reason to change approach. The fix is not a larger iteration cap — it's ensuring every tool_result, success or failure, is returned to the model so it has the information needed to adapt.</blockquote>\n      <h2>Try it</h2>\n      <p>Deliberately break a tool in a small Claude Code or API-based agent loop so it always returns an error, but write the client code to silently swallow that error and feed back an empty success result. Watch how many iterations it takes before the loop either stalls or hits your iteration cap, then fix it by forwarding the real error in the tool_result and observe how quickly the model changes its approach instead.</p>\n    "
  }
];
