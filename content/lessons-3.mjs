// Generated by `node scripts/export-content.mjs` from Redis (content:lessons:3).
// Edit content in Redis and re-run the export, not this file directly.
export const LESSONS = [
  {
    "slug": "3-1-claude-md-hierarchy",
    "title": "CLAUDE.md and the Memory Hierarchy",
    "dek": "How CLAUDE.md files at different scopes combine into Claude Code's context, and why merging is not overriding.",
    "minutes": 7,
    "bodyHtml": "\n      <p>CLAUDE.md is a memory file that Claude Code automatically loads into context at the start of a session, without you having to reference it manually. It typically documents project conventions, build and test commands, architectural constraints, and anything else you want Claude to \"just know\" every time it works in a given codebase. Think of it as persistent, always-on context, distinct from a one-off prompt you type into the session.</p>\n      <p>The exam tests one mechanic here more than any other: the memory hierarchy. Claude Code looks for CLAUDE.md files at multiple scopes, and it loads all of them into context simultaneously.</p>\n      <h2>The four scopes</h2>\n      <ul>\n        <li><strong>Enterprise</strong>: organization-wide policy, managed centrally, applies to every user and every project.</li>\n        <li><strong>User</strong>: personal preferences that follow you across all projects on your machine (for example, \"always write commit messages in imperative mood\").</li>\n        <li><strong>Project</strong>: checked into the repository, shared with every teammate who clones it (build commands, coding conventions, directory layout notes).</li>\n        <li><strong>Local</strong>: project-specific but not committed, for your own private notes or machine-specific paths.</li>\n      </ul>\n      <p>The critical fact: these four scopes are <strong>merged</strong>, not layered as overrides. A more specific file (say, project-level) does not replace or override what an enterprise-level or user-level CLAUDE.md says. It <strong>adds</strong> to it. All applicable CLAUDE.md content ends up concatenated into Claude's context. There is no precedence resolution where the closest file \"wins\" and discards the others.</p>\n      <p>This matters practically. If your enterprise CLAUDE.md says \"never commit directly to main,\" and your project CLAUDE.md doesn't mention branching at all, that enterprise rule is still active in every session inside that project. You cannot silently override an inherited instruction just by staying quiet about it at a lower scope. If you want to change behavior at a lower scope, you have to say so explicitly, and even then both instructions are present in context — good CLAUDE.md authoring means avoiding direct contradictions between scopes, because Claude has to reconcile conflicting text, not automatically prefer the more specific file.</p>\n      <div class=\"callout\"><b>Exam trap</b> Watch for answer choices that describe the hierarchy in override language: \"the project CLAUDE.md overrides the user CLAUDE.md,\" or \"local settings take precedence over and replace project settings.\" This is the single most common wrong-answer pattern in this domain. The correct mental model is additive merging across scopes, not a cascade where specificity wins. If an option uses the word \"overrides\" or \"replaces\" to describe CLAUDE.md scope interaction, it is almost certainly the distractor.</div>\n      <h2>What belongs in CLAUDE.md</h2>\n      <p>Good candidates: the exact commands to run tests and linters, naming conventions, directory structure explanations, known gotchas (\"this repo uses a custom bundler, do not add webpack config\"), and pointers to other docs. Bad candidates: anything that changes frequently, secrets, or content so long it crowds out useful context on every single session — CLAUDE.md content is loaded every time, so it has a real token cost.</p>\n      <blockquote>Scenario: A team has an enterprise CLAUDE.md requiring \"all database migrations must be reviewed by a DBA before merging.\" An engineer, annoyed by this on a low-stakes internal tool, adds a line to the project-level CLAUDE.md saying \"DBA review is not required for this repo.\" What happens? Both instructions are merged into context — Claude sees the conflicting statements together, and behavior becomes unreliable/ambiguous rather than cleanly resolved in favor of the project file. The reasoning: since scopes merge rather than override, the engineer's fix does not work as intended; the enterprise rule was never designed to be locally revoked this way, and if the org wants an exception mechanism, it needs to be built into the enterprise policy itself, not fought at the project level.</blockquote>\n      <h2>Ordering within the merge</h2>\n      <p>Because content from every applicable scope lands in context together, the practical question shifts from \"which one wins\" to \"in what order does Claude read them, and does that create emphasis.\" Claude Code has a defined load order across scopes, but that order affects how instructions are sequenced in context, not which ones are honored. Do not confuse load order with precedence — the exam is testing whether you understand that later-loaded content does not overwrite earlier-loaded content; it accumulates alongside it. If two CLAUDE.md files genuinely disagree, you have an authoring problem, not a resolved conflict, and the fix is to edit the conflicting file, not to rely on scope order to silently pick a winner.</p>\n      <h2>Try it</h2>\n      <p>In an existing project, run <code>claude</code> and check whether a CLAUDE.md exists at the project root. If not, create one with your test command and one architectural note. Then check your home directory for a user-level CLAUDE.md (or create one with a personal preference like your preferred commit message format). Start a new session in that project and ask Claude to state what it knows about the project's conventions — confirm both the project and user content show up together.</p>\n    "
  },
  {
    "slug": "3-2-settings-permissions",
    "title": "settings.json and Permissions",
    "dek": "How settings.json configures permissions, hooks, and environment variables, and how to design a correct allow/deny/ask permission set.",
    "minutes": 7,
    "bodyHtml": "\n      <p>settings.json is the configuration file for Claude Code's behavior — it lives at the user level or the project level and covers permissions, hooks, environment variables, and other runtime behavior. Where CLAUDE.md tells Claude what it should know, settings.json tells Claude Code what it is allowed to do.</p>\n      <h2>Permission rules</h2>\n      <p>Permissions are expressed as rules that fall into three categories: <strong>allow</strong>, <strong>deny</strong>, and <strong>ask</strong>. Each rule targets a tool or a specific command pattern — for example, allowing <code>Bash(npm test:*)</code> while denying <code>Bash(rm -rf:*)</code>. When Claude Code is about to use a tool, it checks these rules to decide whether to proceed silently, block outright, or pause and prompt the user for confirmation.</p>\n      <p>The exam frames correct permission design as a specific design principle: the narrowest permission set that still lets the workflow complete. This is not the same as \"deny everything by default\" and it is not the same as \"allow everything for convenience.\" Both extremes are wrong answers in different ways.</p>\n      <ul>\n        <li><strong>Blanket allow</strong> (e.g., allowing all Bash commands) removes friction but also removes the safety net — a single hallucinated or misinterpreted instruction can run a destructive command with no checkpoint.</li>\n        <li><strong>Blanket ask</strong> (prompting for every single tool call) technically keeps a human in the loop but destroys the productivity benefit of an agentic workflow — you end up rubber-stamping dozens of prompts per session, which trains people to click \"yes\" reflexively, defeating the purpose of the check.</li>\n        <li><strong>Narrowest workable set</strong> means allowing the specific, low-risk, high-frequency operations a given workflow actually needs (running the test suite, reading files, formatting code), asking for medium-risk or ambiguous operations, and denying operations that should never happen in this project regardless of context (pushing to main, deleting the database, curl-ing to arbitrary external hosts).</li>\n      </ul>\n      <div class=\"callout\"><b>Exam trap</b> A common distractor pattern presents two options: one that allows broad tool categories \"to reduce interruptions\" and one that denies almost everything \"to be safe,\" and asks you to pick the better practice. Neither is correct — the right answer is almost always the option describing scoped, workflow-specific allow rules combined with deny rules for genuinely dangerous operations, leaving ask as the fallback for the gray area in between. If an option describes permissions as all-or-nothing, it is wrong.</div>\n      <h2>Where settings live</h2>\n      <p>settings.json can exist at the user level (applies across all your projects) and at the project level (checked into the repo, shared with the team, typically the place for permissions that reflect what's actually safe in this specific codebase). Project-level settings.json is how a team standardizes what an agent may do in their repo without relying on every individual engineer remembering to configure it themselves — this is distinct from CLAUDE.md, which shapes what Claude knows, not what it's allowed to execute.</p>\n      <p>settings.json is also where hooks get registered and where environment variables for the Claude Code process get set — permissions are the piece most heavily tested, but don't lose sight of the fact that this one file is the general behavior-configuration surface, not a permissions-only file.</p>\n      <blockquote>Scenario: A team wants Claude Code to autonomously run their test suite and linter, read any file in the repo, and propose git commits, but they never want it to push directly to any remote branch or install new npm packages without asking. What's the correct settings.json design? Allow <code>Bash(npm test:*)</code>, <code>Bash(npm run lint:*)</code>, and file-read tools; set <code>Bash(git push:*)</code> to deny (not ask — \"never\" means never, and ask still permits a tired reviewer to approve it by habit); set package installation commands to ask, since it's a judgment call that depends on which package. This mixes all three rule types deliberately rather than defaulting to one.</blockquote>\n      <h2>Rule specificity</h2>\n      <p>Permission rules can be scoped narrowly, down to a specific command pattern, rather than only at the level of a whole tool. Allowing all of <code>Bash</code> is very different from allowing only <code>Bash(npm test:*)</code> — the exam expects you to recognize that a rule written at the tool level (all Bash) is almost never the narrowest workable set, because \"Bash\" covers everything from running a linter to deleting a partition. Well-designed permission sets tend to have many small, specific allow rules rather than one broad one, precisely because that specificity is what lets you allow the 90 percent of commands that are routine while still asking or denying the ones that aren't.</p>\n      <h2>Try it</h2>\n      <p>Open (or create) <code>.claude/settings.json</code> in a real project. Add an allow rule for your test command, a deny rule for a command you never want run automatically (like <code>git push --force</code>), and leave everything else on the default ask behavior. Start a session and try to trigger each rule to confirm the permission system responds the way you configured it.</p>\n    "
  },
  {
    "slug": "3-3-slash-commands-vs-skills",
    "title": "Slash Commands vs. Skills",
    "dek": "The key distinction between user-triggered slash commands and Claude-triggered skills, and when to build each.",
    "minutes": 6,
    "bodyHtml": "\n      <p>Slash commands and skills both package reusable capability, and both get confused with each other constantly on this exam. The distinguishing question is always: <strong>who decides when this runs?</strong></p>\n      <h2>Slash commands</h2>\n      <p>A slash command is a reusable prompt template that the user invokes explicitly by typing <code>/name</code>. You write the template once — maybe it's <code>/review-pr</code> that expands into a detailed prompt asking Claude to check a diff against your team's style guide — and from then on, any time you (or a teammate) wants that exact workflow, you type the command. The user is the trigger. Claude never decides on its own to run a slash command; it waits to be invoked.</p>\n      <p>Slash commands are appropriate for workflows you perform repeatedly and want fast, predictable, on-demand access to — things where you always know in advance when you want them.</p>\n      <h2>Skills</h2>\n      <p>A skill is a packaged, discoverable capability with its own instructions, and possibly its own resources or scripts, that Claude invokes <strong>autonomously</strong> when it judges the current task matches the skill's purpose. You don't type anything to trigger a skill — Claude reads the task at hand, considers the skills available to it, and decides for itself whether one applies. The decision-maker is Claude, not the user.</p>\n      <p>Skills are appropriate when you can't predict in advance exactly when the capability is needed, or when you want Claude to reach for specialized behavior without the user having to remember a specific command name. A skill for \"how to write database migrations in this repo's style\" is more useful as a skill than a slash command, because Claude should reach for it any time a migration task comes up — including tasks the user described in plain language without knowing a migration-specific command exists.</p>\n      <div class=\"callout\"><b>Exam trap</b> Scenario questions often describe a capability and ask whether a slash command or a skill is the better fit, without using the words \"slash command\" or \"skill\" directly — you have to infer it from who needs to make the trigger decision. A description like \"Claude should notice on its own when a task involves X and handle it appropriately\" points to a skill. A description like \"the team wants a fast, consistent way to kick off their release checklist by typing a short command\" points to a slash command. Answering based on surface features (e.g., \"it involves multiple steps, so it must be a skill\") instead of the trigger-ownership question is the trap — either construct can be simple or complex; the deciding factor is who initiates.</div>\n      <h2>They can coexist</h2>\n      <p>These aren't mutually exclusive tools competing for the same job — a mature Claude Code setup often has both. A team might have a skill that lets Claude autonomously recognize \"this looks like a security-sensitive change\" and apply extra scrutiny, while also having an explicit <code>/security-review</code> slash command for when someone wants to force that same scrutiny on demand, on a diff Claude might not have flagged on its own.</p>\n      <blockquote>Scenario: An engineer wants Claude to automatically follow their company's API-versioning conventions any time it edits a public API file, without having to remind Claude every session. They also want a way to explicitly ask Claude to generate a full changelog entry when they're ready to ship. Which construct fits which need? The API-versioning behavior should be a skill — it needs to activate based on Claude noticing the file context, not based on the user remembering to ask. The changelog generation should be a slash command — the user knows exactly when they want it (at ship time) and wants a predictable, explicitly-triggered action, not something Claude guesses at.</blockquote>\n      <h2>Naming and discoverability</h2>\n      <p>Because a user has to remember a slash command's name to invoke it, naming matters — an obscure or inconsistent name means the command effectively doesn't exist for anyone who forgot it. Skills don't have this problem in the same way, since Claude is the one scanning available skills against the task at hand, but that shifts the burden elsewhere: a skill's description needs to be written clearly enough that Claude can correctly judge relevance. A vaguely described skill either gets invoked when it shouldn't be, or ignored when it should apply. When you're authoring either construct, keep in mind who has to correctly recognize when to use it — the user reading a command list, or Claude reading a skill description — and write for that audience.</p>\n      <h2>Try it</h2>\n      <p>Create a slash command in <code>.claude/commands/</code> for a workflow you do often, such as summarizing recent git history into a changelog draft, and invoke it with <code>/</code> plus the file name. Then write a minimal skill (a directory with instructions describing when it applies) for a narrower, context-triggered task, and test whether Claude picks it up on its own during a relevant task without you naming it explicitly.</p>\n    "
  },
  {
    "slug": "3-4-hooks",
    "title": "Hooks for Deterministic Behavior",
    "dek": "Why hooks exist to enforce hard requirements that prompted instructions can't reliably guarantee, and how lifecycle events work.",
    "minutes": 7,
    "bodyHtml": "\n      <p>A hook is a shell command that fires automatically when a specific lifecycle event occurs during a Claude Code session — for example, before a tool runs, after a tool runs, or when the session stops. Hooks are configured in settings.json and execute deterministically: the shell command runs every single time the event fires, with no judgment call and no possibility of being skipped because Claude \"forgot\" or reasoned its way around it.</p>\n      <h2>Why hooks exist</h2>\n      <p>The core justification the exam wants you to internalize: prompted instructions are probabilistic, hooks are deterministic. If you put \"please always run the linter before committing\" in CLAUDE.md, that's a strong nudge, and most of the time Claude will follow it. But it's still a natural-language instruction being interpreted by a model — under unusual circumstances, competing instructions, or a long context window, it can be skipped. For anything that constitutes a hard requirement — something that must happen every time, no exceptions — a hook removes the ambiguity, because it's the harness executing a fixed shell command, not the model deciding whether to comply.</p>\n      <p>This is the distinction to hold onto: CLAUDE.md and prompts shape what Claude is inclined to do; hooks shape what unconditionally happens. If a requirement can tolerate occasional misses, a strongly worded instruction may be sufficient. If it cannot — for example, \"never allow a commit that fails the test suite\" — that belongs in a hook, not a hope.</p>\n      <h2>Common lifecycle events</h2>\n      <ul>\n        <li><strong>PreToolUse</strong>: fires before a tool call executes — useful for validating or blocking an action before it happens (e.g., rejecting a Bash command that matches a dangerous pattern, even one permissions didn't catch).</li>\n        <li><strong>PostToolUse</strong>: fires after a tool call completes — useful for automatically running a formatter or linter right after a file edit, without relying on Claude remembering to do it.</li>\n        <li><strong>Stop</strong>: fires when Claude finishes responding — useful for enforcing \"run the full test suite before ending the turn\" as an unconditional check.</li>\n      </ul>\n      <p>Hooks receive information about the event and can, depending on the event and exit behavior, block the action, inject feedback, or simply log and continue. The key exam point isn't memorizing every event name — it's recognizing that hooks are the deterministic enforcement layer, distinct from the persuasive layer (CLAUDE.md, prompts, skills).</p>\n      <div class=\"callout\"><b>Exam trap</b> A frequent wrong answer pattern presents a hard requirement — \"the pipeline must never deploy without a passing test suite\" — and offers a fix phrased as \"add a strong instruction to CLAUDE.md telling Claude to always run tests first.\" That sounds reasonable but is the weaker mechanism for a hard requirement, because CLAUDE.md content is advisory context the model interprets, not an enforced gate. The correct answer for genuine \"must always happen\" requirements is a hook (e.g., a PreToolUse or Stop hook that runs the test command and blocks on failure), not a memory file instruction, no matter how emphatically worded.</div>\n      <blockquote>Scenario: A team keeps finding that Claude occasionally commits code with an unformatted file, despite CLAUDE.md explicitly saying \"always run the formatter before committing.\" What's the fix? Replace or supplement the instruction with a PostToolUse hook that runs the formatter automatically after every file edit, or a PreToolUse hook on the commit command that blocks if formatting is out of date. The reasoning: the team already tried the prompted-instruction approach and it's not reliable enough, which is exactly the signal that this requirement needs deterministic enforcement instead of a stronger-worded reminder.</blockquote>\n      <h2>Hooks versus permissions</h2>\n      <p>Hooks and permission rules can look similar on the surface — both can block an action — but they solve different problems. A deny permission rule stops a specific tool call from happening at all. A hook can do that too (a PreToolUse hook that exits with a blocking status), but hooks can also run arbitrary logic beyond a simple allow/deny check: validating file contents, checking external state, or injecting extra context back into the conversation. Think of permissions as a fixed rulebook keyed on tool and command pattern, and hooks as a more general enforcement mechanism that can execute real logic at defined points in the lifecycle, including logic that has nothing to do with permissions at all, like automatically formatting a file after every edit.</p>\n      <h2>Try it</h2>\n      <p>Add a PostToolUse hook in your project's settings.json that runs your formatter (or even just <code>echo</code> a message) after any file-editing tool is used. Trigger it by asking Claude to edit a file, and confirm the hook fires every time regardless of what else is happening in the session — that unconditional firing is the entire point.</p>\n    "
  },
  {
    "slug": "3-5-plan-mode",
    "title": "Plan Mode and Human Review",
    "dek": "When to use plan mode's read-only research-then-approve workflow for risky or hard-to-reverse changes.",
    "minutes": 6,
    "bodyHtml": "\n      <p>Plan mode is a read-only mode in Claude Code: while it's active, Claude can research the codebase (read files, search, run non-mutating commands) but cannot make edits or run mutating commands. Instead, Claude uses that research to produce a proposed plan of action, which a human reviews and explicitly approves before any actual change happens.</p>\n      <p>The purpose is to insert a deliberate checkpoint before irreversible or high-blast-radius work begins. Once the plan is approved, Claude exits plan mode and executes — but the execution phase only starts after a human has seen the intended approach and signed off, rather than discovering what happened after the fact by reading a diff.</p>\n      <h2>When plan mode is the right call</h2>\n      <p>The exam expects you to match plan mode to a specific risk profile: changes that are risky, broad in scope, or hard to reverse, where getting the approach wrong is expensive to undo. Examples: a schema migration touching a production database, a refactor spanning dozens of files, changes to authentication or billing logic, or any change where the \"best approach\" is genuinely ambiguous and worth debating before code gets written.</p>\n      <p>Plan mode is not intended as the default mode for routine work. Using it for every trivial edit (fixing a typo, adding a log line) adds friction without adding safety value, since there's nothing risky to review. The judgment call is proportional: the more consequential and less reversible the change, the stronger the case for forcing a plan-then-approve cycle rather than letting Claude edit directly.</p>\n      <div class=\"callout\"><b>Exam trap</b> Exam questions sometimes conflate plan mode with permissions (deny/ask rules) or with hooks. They are different mechanisms solving different problems. Permissions gate individual tool calls based on static rules; hooks deterministically enforce lifecycle behavior; plan mode is specifically about front-loading a human review of Claude's overall approach before execution begins, for a single unit of risky work. If an answer choice describes plan mode as \"blocking a specific dangerous command,\" that's actually describing a permission deny rule or a hook, not plan mode. Plan mode's distinguishing trait is the propose-then-approve sequence at the level of an entire approach, not a single tool call.</div>\n      <h2>What plan mode does not replace</h2>\n      <p>Plan mode doesn't substitute for good permissions or hooks — it's a complementary layer. Even after a plan is approved, sensible permission rules and any relevant hooks still apply during execution. Plan mode addresses \"is this the right approach,\" not \"is this specific command safe to run,\" which is what permissions and hooks separately handle.</p>\n      <blockquote>Scenario: An engineer asks Claude to \"migrate our user table to add a new required column with a backfill, across a database with 40 million rows in production.\" Should this run directly, or start in plan mode? Plan mode is the right call: this is a hard-to-reverse, high-blast-radius change (a bad migration on a production table with 40 million rows is expensive or impossible to cleanly undo), and there are multiple reasonable approaches (online migration strategy, batching, locking behavior) worth a human evaluating before any SQL executes. Contrast with \"rename this local variable for clarity\" — low risk, trivially reversible, no need for a plan-approval cycle.</blockquote>\n      <h2>What \"read-only\" actually means</h2>\n      <p>During plan mode, Claude can still use tools that gather information: reading files, searching the codebase, running commands that inspect state without changing it (checking git log, running a dry-run flag, querying a schema). What it cannot do is anything that mutates the repository or environment — editing files, running migrations, installing packages, committing. This distinction matters because plan mode is not \"Claude can't do anything,\" it's \"Claude can look but not touch,\" which is precisely why the research it does during planning tends to be more thorough than a plan proposed without that investigation — the plan is grounded in what Claude actually found in the code, not just a guess based on the request.</p>\n      <h2>Approval is not a formality</h2>\n      <p>The value of plan mode collapses if approval becomes reflexive. A human skimming a plan and clicking approve without actually evaluating the proposed approach gets none of the safety benefit and only the added latency. The exam frames plan mode's value around genuine human judgment being applied at the checkpoint — catching a wrong assumption, an overlooked edge case, or an approach that's technically workable but not what the team actually wants — not around the mere existence of a pause in the workflow.</p>\n      <h2>Try it</h2>\n      <p>Pick a real but nontrivial task in one of your projects — something like restructuring a module or changing a shared interface. Enter plan mode, let Claude research and propose an approach without editing anything, and actually read the plan critically before approving it. Notice what the read-only research phase catches that you'd have otherwise only discovered after the edits were already made.</p>\n    "
  },
  {
    "slug": "3-6-cicd-headless",
    "title": "CI/CD and Headless Claude Code",
    "dek": "Running Claude Code non-interactively in pipelines, and why permissions must be fully configured before the run starts.",
    "minutes": 7,
    "bodyHtml": "\n      <p>Headless mode runs Claude Code non-interactively — no terminal session where a human is watching and available to answer prompts. This is the mode used to embed Claude Code into CI/CD pipelines: a build step that asks Claude to review a diff, generate release notes, fix a failing test, or perform some other bounded task as part of an automated workflow, triggered by something like a pull request or a scheduled job rather than a person typing at a prompt.</p>\n      <h2>The core constraint: no fallback prompt</h2>\n      <p>In an interactive session, when Claude Code hits a permission rule set to <code>ask</code>, it pauses and waits for you to type yes or no. In headless mode, there is no one there to answer. This is the single most important operational fact about CI/CD usage: <strong>permissions must be fully configured up front</strong>, before the run starts, because there's no interactive fallback mid-run. An <code>ask</code> rule that would have paused politely in your terminal instead becomes a stall or a failure in a pipeline — the run has no mechanism to resolve it.</p>\n      <p>Practically, this means a headless configuration needs its allow and deny rules to be exhaustive enough that the pipeline's task can complete without ever hitting an unresolved <code>ask</code>. This pushes teams toward being more deliberate about scoping permissions for CI-specific settings.json than they might be for an interactive developer session, precisely because there's no human safety net available if the scoping is wrong.</p>\n      <div class=\"callout\"><b>Exam trap</b> A common distractor suggests that headless/CI usage is just \"the same as interactive Claude Code but automated,\" implying permission prompts still function the same way. They don't functionally resolve the same way — an unanswered ask prompt in a non-interactive run doesn't get politely deferred, it blocks progress or causes the run to fail outright, since nothing is listening for a reply. Another trap: suggesting the fix for CI is to allow everything to avoid stalls. That reintroduces the blanket-allow problem from permission design, just in a higher-stakes environment (an unattended pipeline with write access to your repo and possibly your infrastructure). The correct answer is precise upfront scoping, not blanket allow and not leaving ask rules in place.</div>\n      <h2>What headless runs are good for</h2>\n      <p>Bounded, well-defined tasks with a clear success condition work best: automated PR review comments, generating a changelog from commits, running a fix-and-verify loop against a failing test where success is objectively checkable, or triaging an issue and proposing a labeled response. Open-ended, judgment-heavy tasks are a worse fit for headless mode precisely because there's no human present to redirect Claude if its approach goes sideways mid-task — which is also why plan-mode-style human checkpoints don't translate directly into a headless pipeline; the checkpoint has to be replaced by tight upfront scoping instead.</p>\n      <h2>Subagents in pipelines</h2>\n      <p>CI/CD tasks are also a natural place to use subagents — separately configured agents with their own system prompt and restricted tool access, delegated a scoped piece of work by a main agent. A pipeline might use a narrowly-scoped subagent purely for running and interpreting test output, with no permission to touch deployment configuration, keeping the blast radius of that piece of the pipeline small even within an already-headless run.</p>\n      <blockquote>Scenario: A team wants to add a CI step where Claude Code automatically reviews every pull request and posts a comment, but their existing settings.json has several permission rules set to ask because that's fine for their local interactive workflow. What breaks when they wire this into CI as-is, and what's the fix? The ask rules will stall or fail the pipeline step since there's no one to answer the prompt in a CI runner. The fix is a CI-specific settings.json (or override) that converts every rule the pipeline actually needs from ask to a precise allow, and leaves genuinely dangerous operations as deny rather than defaulting to allow just to unblock the run.</blockquote>\n      <h2>Try it</h2>\n      <p>Run Claude Code in headless mode locally against a small, bounded task (check your CLI's non-interactive invocation flag), using a settings.json where every permission the task needs is explicitly allowed and everything else is denied. Confirm the run completes without ever needing you to answer a prompt, then deliberately remove one needed allow rule and observe how the run fails or stalls instead of asking you.</p>\n    "
  }
];
